DB_HOST=localhost
DB_PORT=3306
DB_NAME=yelp_data
DB_USER=root
DB_PASSWORD=
DB_CHARSET=utf8mb4

# FastAPI CORS: production에서 React와 API가 같은 origin이면 비워 둡니다.
# 다른 origin일 때만 정확한 https origin을 쉼표로 구분해 지정합니다.
RETENTION_ENV=development
RETENTION_ALLOWED_ORIGINS=
RETENTION_ALLOW_DEV_OPERATOR=0
