# AGENTS.md

## 1. 프로젝트 목적

이 프로젝트는 Yelp의 음식 리뷰 공급을 다음 세 축으로 관리하는 운영 서비스다.

1. 지역별 리뷰 공급 변화 및 이상 탐지
2. 기존 핵심 리뷰어의 활동 약화·중단 예측과 우선 검토
3. 신규 핵심 리뷰어 유입 변화 관찰 및 우선 검토 대상 연결

최종 결과물은 단순 분석 대시보드가 아니라, Yelp 플랫폼 운영팀이 공급 변화를
감지하고 근거를 확인한 뒤 개인 특별 관리 또는 지역 활성화 운영안을 결정·기록하는
`리뷰어 리텐션 운영 서비스`다.

주요 사용자는 콘텐츠·커뮤니티 운영자와 리뷰어 CRM 담당자다.

---

## 2. 도메인 표현 원칙

- 최신 사용자 화면과 제품 문서에서는 `파워 리뷰어` 대신 `핵심 리뷰어`를 사용한다.
- `핵심 리뷰어`는 Yelp `Elite` 사용자와 같은 뜻이 아니며, 선정 연도의 음식 관련
  리뷰 수와 활동 월 기준으로 정의한 분석 코호트다.
- 과거 의사결정 문서의 제목·파일명, 코드 변수, 데이터 컬럼에 남은
  `power_reviewer`·`파워 리뷰어`는 재현성과 이력 추적을 위해 유지할 수 있다.
- 리뷰 데이터로 확인할 수 없는 방문 활동 중단이나 서비스 완전 이탈로 확대 해석하지 않는다.
- `미식 번아웃`은 의학적 진단이 아니라 리뷰 활동 변화로 관찰되는 운영상 위험 신호다.
- `생활 반경` 대신 `맛집 탐방 반경` 또는 `리뷰 활동 반경`을 사용한다.
- `risk_score`는 보정된 이탈 확률이 아니라 위험 순위를 산정하기 위한 모델 점수다.
- 따라서 `이탈 가능성 84%`처럼 확률로 단정하지 않는다.
- 데이터로 확인할 수 없는 사용자의 거주지, 직장, 실제 생활 변화를 추론하지 않는다.

---

## 3. 데이터 및 모델 보호 원칙

- 기존 데이터 파이프라인, 코호트 정의, 시간 분할 및 모델 결과를 임의로 변경하지 않는다.
- 변경이 필요하면 먼저 변경 이유와 영향을 설명하고 사용자 확인을 받는다.
- 미래 시점 정보를 피처에 포함하지 않으며 시간 누수를 방지한다.
- 실제 데이터가 존재하면 실제 데이터를 우선 사용한다.
- 데모·합성 데이터를 사용하면 화면에서 명확하게 표시한다.
- 데이터로 계산되지 않은 수치나 모델 결과를 임의로 생성하지 않는다.

---

## 4. 프로젝트 경로

- 현재 운영 서비스(React) 개발 경로는 `app/`이다. `app/`은 `api/`(FastAPI)를 통해
  MySQL(`yelp_data`)을 직접 조회한다 — `app/src/data/*.json`은 API가 아직 없던
  시절의 정적 폴백으로만 남아 있다.
- 위험유형·근거·전략 판단, 리뷰어 프로필 정규화, React JSON 직렬화 로직의
  기준(source of truth)은 `shared/retention/`이다. Streamlit
  (`archive/app_streamlit_v04/`), `api/`, `scripts/export_frontend_data.py`
  전부 이 모듈만 참조하며, 각자 다시 구현하지 않는다.
- `archive/app_streamlit_v04/core/`는 Streamlit UI·데이터 로딩(`core/data.py`의
  `load_app_data` 등)과, 기존 `from core.insights import ...`처럼 이미 있던
  import 경로를 계속 지원하기 위한 호환 wrapper로 남아 있다. 새 코드는
  `shared.retention`을 직접 참조하고 이 경로를 거치지 않는다.
- `scripts/export_frontend_data.py`는 `app/src/data/*.json`(폴백용 정적 JSON)을
  만드는 스크립트로 유지한다. 모델·프로파일이 갱신되면 재실행한다.
- 2026-07-30 기준으로 React의 런타임 데이터 경로는 정적 JSON 폴백이 아니라
  `api/` → MySQL이다. 위 JSON은 API 응답의 정합성 기준값·복구용 export 산출물로
  보존하며, 현재 React 코드에는 API 실패 시 JSON을 자동으로 다시 읽는 기능이 없다.
- 전환 전 기록(2026-07-30 DB/API 전환 이전): 모델·데이터 파생 로직의 기준
  (source of truth)은 `archive/app_streamlit_v04/`의 Streamlit 앱이었고,
  `app/`(React)은 `scripts/export_frontend_data.py`로 이 로직을 재사용해
  생성한 정적 JSON을 읽었다. 이 기록은 이전 구조를 이해하기 위해 보존한다.
- `archive/app_streamlit_v01_prototype/`(과거 분석 프로토타입)은 수정하지 않는다.
- 현재 v04 코호트와 v05 모델의 분석 설정값은 `configs/analysis_config_v04.yaml`을
  우선 확인한다. `configs/analysis_config.yaml`은 이전 v01 비교 설정으로 보존한다.
- 프로젝트의 세부 정의와 현재 상태는
  `docs/07_history_and_handoff/CODEX_HANDOFF.md`를 확인한다.
- 확정된 의사결정은 `docs/06_decisions/`의 문서를 따른다.
- Finalization 범위·담당·완료 조건은
  `docs/06_decisions/DEC-015_finalization_scope_and_ownership.md`를 따른다.
- Finalization 브랜치·PR·검증 순서는
  `docs/07_history_and_handoff/FINALIZATION_EXECUTION_PLAN.md`를 따른다.

문서 내용이 충돌하면 다음 순서로 판단한다.

1. `AGENTS.md`
2. `docs/06_decisions/`
3. `docs/07_history_and_handoff/CODEX_HANDOFF.md`
4. 요구사항 및 기타 참고 문서

충돌을 임의로 해석하기 어려우면 수정 전에 사용자에게 확인한다.

---

## 5. 현재 React 서비스 개발 원칙

- 분석 결과를 나열하는 화면보다 운영자의 판단과 행동 흐름을 우선한다.
- 페이지별 목적에 맞는 정보 구조를 사용하고 같은 대시보드 레이아웃을 반복하지 않는다.
- 불필요한 카드, 테두리, 설명 문구를 반복하지 않는다.
- 실제 구현된 기능과 향후 기능을 시각적으로 구분한다.
- 데이터가 부족한 향후 기능은 `고도화 예정`으로 표시한다.
- 구현되지 않은 버튼이나 기능을 실제 작동하는 것처럼 표현하지 않는다.
- HTML·CSS 코드가 화면에 문자열로 노출되지 않도록 한다.
- 데스크톱 발표 화면을 우선하되 작은 화면에서도 주요 정보가 잘리지 않게 한다.
- 현재 화면 변경은 `app/` React에서만 수행한다.
- `archive/app_streamlit_v04/`와 `archive/app_streamlit_v01_prototype/`은 비교·이력
  보존 대상이며 Finalization 작업에서 수정하지 않는다.

---

## 6. 작업 및 검증 원칙

- 기존 기능과 데이터 연결을 먼저 파악한 후 수정한다.
- 요청 범위와 관계없는 파일은 수정하지 않는다.
- 공통 스타일과 컴포넌트는 가능한 한 재사용할 수 있게 구성한다.
- React 화면 수정 후 lint·build와 변경 경로의 브라우저 검증을 수행한다.
- 오류, 빈 화면, 잘린 요소, HTML 문자열 노출 여부를 확인한다.
- 작업 완료 시 변경 파일, 주요 변경 사항, 검증 결과를 보고한다.

---

## 7. Finalization 실행 계약

- 현재 Finalization에서는 DEC-015가 `필수`로 분류한 항목만 구현한다.
- Consumer 기능, 실제 효과 측정, 자동 재학습, 추가 모델 실험은 현재 릴리스 범위에서
  제외하고 고도화 항목으로 유지한다.
- A 영역의 앱 안정성·보안 후속 작업은 김동섭 담당 브랜치를 우선한다.
- 모델 구조, 코호트, 시간 분할, 성능 수치와 산출물을 임의로 변경하지 않는다.
  C 영역의 문서 정합성만 지정 담당자와 근거를 확인한 별도 PR에서 수정한다.
- 모든 작업 브랜치는 최신 `release/finalization`에서 시작하고 중간 PR의 base도
  `release/finalization`으로 한다. `main`에 직접 커밋하거나 push하지 않는다.
- 공용 파일은 공동 소유로 두지 않고 DEC-015와 실행 계획에 정한 PR 순서대로
  순차 수정한다. 선행 PR이 머지되기 전에는 후행 PR에서 같은 파일을 수정하지 않는다.
- 허용 범위 밖 파일 변경이 필요하면 수정하지 말고 경로·이유·영향을 먼저 보고한다.
